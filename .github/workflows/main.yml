name: CI (Windows self-hosted)

run-name: "CI (${{ github.event_name }}: ${{ github.ref_name }})"

on:
  push:
  pull_request:
  workflow_dispatch:
    inputs:
      run_lint:
        description: "Run lint (pre-commit)"
        type: boolean
        default: false
      run_fast:
        description: "Run fast deterministic tests"
        type: boolean
        default: true
      run_curated:
        description: "Run curated unit test list"
        type: boolean
        default: false
      run_integration:
        description: "Run integration tests"
        type: boolean
        default: false
      run_ui:
        description: "Run UI / QML tests"
        type: boolean
        default: false

env:
  PYTHON_VERSION: "3.11"
  PYTHONHASHSEED: "0"
  PIP_DISABLE_PIP_VERSION_CHECK: "1"
  PYTEST_DISABLE_PLUGIN_AUTOLOAD: "1"

  # Secrets presence flags (safe: only checks existence)
  HAS_MARKETPLACE_KEYS: ${{ secrets.MARKETPLACE_HMAC_KEY != '' }}
  HAS_LICENSING_KEYS: ${{ secrets.LICENSING_PRIVATE_KEY != '' }}

jobs:
  smoke-harness:
    name: Smoke harness (env + first failure)
    runs-on: [self-hosted, windows]
    timeout-minutes: 25

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install deps
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
          python -m pip install pytest

      - name: Ensure ProcDump is available
        shell: pwsh
        run: |
          $procdump = Get-Command procdump.exe -ErrorAction SilentlyContinue
          if (-not $procdump) {
            $procdumpDir = "tools/procdump"
            New-Item -ItemType Directory -Force $procdumpDir | Out-Null
            $zipPath = Join-Path $procdumpDir "procdump.zip"
            Invoke-WebRequest -Uri "https://download.sysinternals.com/files/Procdump.zip" -OutFile $zipPath
            Expand-Archive -Path $zipPath -DestinationPath $procdumpDir -Force
            Add-Content $env:GITHUB_PATH $procdumpDir
          }
          $procdump = Get-Command procdump.exe -ErrorAction SilentlyContinue
          if ($procdump) {
            $ver = (Get-Item $procdump.Source).VersionInfo.FileVersion
            Write-Host "ProcDump: $($procdump.Source)"
            if ($ver) { Write-Host "ProcDump version: $ver" }
          }

      - name: Environment snapshot
        shell: pwsh
        run: |
          python -V
          python -m pip list --format=columns

      - name: Pytest (first failure) + crash dump on AV
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force var/ci | Out-Null
          New-Item -ItemType Directory -Force var/crashdumps | Out-Null

          if (-not $env:DUDZIAN_DISABLE_QTCHARTS) {
            $env:DUDZIAN_DISABLE_QTCHARTS = "1"
          }

          $procdump = Get-Command procdump.exe -ErrorAction SilentlyContinue
          if ($procdump) {
            & $procdump.Source `
              -accepteula `
              -ma `
              -e `
              -n 1 `
              -x var/crashdumps `
              python -m pytest -vv --maxfail=1 -x --junitxml=var/ci/smoke_harness_first.xml
            exit $LASTEXITCODE
          }

          pytest -vv --maxfail=1 -x --junitxml=var/ci/smoke_harness_first.xml

      - name: Upload smoke harness diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: smoke-harness-diagnostics
          path: |
            var/ci/**
            var/crashdumps/**
            var/crashdumps/**/*.dmp
          if-no-files-found: warn
          retention-days: 5

  smoke-harness-all:
    name: Smoke harness (env + all failures)
    runs-on: [self-hosted, windows]
    needs: smoke-harness
    if: always()
    timeout-minutes: 25

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install deps
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
          python -m pip install pytest

      - name: Environment snapshot
        shell: pwsh
        run: |
          python -V
          python -m pip list --format=columns

      - name: Pytest (all failures)
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force var/ci | Out-Null
          pytest -vv --junitxml=var/ci/smoke_harness_all.xml

      - name: Upload smoke harness (all) report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: smoke-harness-all-report
          path: |
            var/ci/**
          if-no-files-found: warn
          retention-days: 5

  lint:
    name: Lint (pre-commit)
    needs: smoke-harness
    runs-on: [self-hosted, windows]
    if: github.event_name == 'workflow_dispatch' && inputs.run_lint
    timeout-minutes: 25

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Pre-commit
        shell: pwsh
        run: |
          python -m pip install -e ".[dev]"
          python -m pip install pre-commit
          pre-commit run --all-files --show-diff-on-failure

  fast-tests:
    name: Fast tests (neutral)
    needs: smoke-harness
    runs-on: [self-hosted, windows]
    if: |
      github.event_name != 'workflow_dispatch' ||
      inputs.run_fast
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install deps
        shell: pwsh
        run: |
          python -m pip install -e ".[dev]"
          python -m pip install pytest

      - name: Generate stubs (optional)
        shell: pwsh
        run: |
          if (Test-Path "scripts/generate_trading_stubs.py") {
            python scripts/generate_trading_stubs.py --skip-cpp
            if ($LASTEXITCODE -ne 0) {
              Write-Host "Stub generation failed (ignored)."
            }
          } else {
            Write-Host "No stub generator found; skipping."
          }

      - name: Pytest fast
        shell: pwsh
        run: |
          pytest --maxfail=1 `
            -m "not qml and not integration and not e2e_demo_paper and not soak and not performance" `
            --ignore=tests/integration `
            --ignore=tests/e2e

  curated-tests:
    name: Curated unit suite
    needs: fast-tests
    runs-on: [self-hosted, windows]
    if: github.event_name == 'workflow_dispatch' && inputs.run_curated
    timeout-minutes: 40

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install deps
        shell: pwsh
        run: |
          python -m pip install -e ".[dev]"
          python -m pip install pytest

      - name: Pytest curated
        shell: pwsh
        run: |
          pytest `
            tests/test_pipeline_paper.py `
            tests/test_risk_profiles.py `
            tests/test_multi_strategy_scheduler.py `
            tests/test_trading_decision_journal.py

  integration-tests:
    name: Integration tests
    needs: fast-tests
    runs-on: [self-hosted, windows]
    if: github.event_name == 'workflow_dispatch' && inputs.run_integration
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install deps
        shell: pwsh
        run: |
          python -m pip install -e ".[dev]"
          python -m pip install pytest

      - name: Pytest integration
        shell: pwsh
        run: |
          pytest -m integration --maxfail=1

  ui-tests-offscreen:
    name: UI / QML tests (offscreen)
    needs: fast-tests
    runs-on: [self-hosted, windows]
    if: github.event_name == 'workflow_dispatch' && inputs.run_ui
    timeout-minutes: 45
    env:
      QT_QPA_PLATFORM: offscreen
      QT_OPENGL: software

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install deps
        shell: pwsh
        run: |
          python -m pip install -e ".[dev]"
          python -m pip install pytest pytest-xdist

      - name: Ensure ProcDump is available
        shell: pwsh
        run: |
          $procdump = Get-Command procdump.exe -ErrorAction SilentlyContinue
          if (-not $procdump) {
            $procdumpDir = "tools/procdump"
            New-Item -ItemType Directory -Force $procdumpDir | Out-Null
            $zipPath = Join-Path $procdumpDir "procdump.zip"
            Invoke-WebRequest -Uri "https://download.sysinternals.com/files/Procdump.zip" -OutFile $zipPath
            Expand-Archive -Path $zipPath -DestinationPath $procdumpDir -Force
            Add-Content $env:GITHUB_PATH $procdumpDir
          }

      - name: Check UI deps
        shell: pwsh
        run: |
          python -c "import importlib.util, sys; sys.exit(0 if importlib.util.find_spec('PySide6') else 1)"
        continue-on-error: true

      - name: Pytest UI
        shell: pwsh
        run: |
          ./scripts/run_qml_tests_windows.ps1 -QtQpaPlatform "offscreen" -QtOpenGL "${{ env.QT_OPENGL }}"

      - name: Upload QML diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: qml-test-diagnostics-offscreen
          path: |
            test-results/qml/**
            var/crashdumps/**
            var/crashdumps/**/*.dmp
          if-no-files-found: warn
          retention-days: 5

  ui-tests-windows:
    name: UI / QML tests (windows)
    needs: fast-tests
    runs-on: [self-hosted, windows]
    if: >
      github.event_name == 'workflow_dispatch' &&
      inputs.run_ui &&
      vars.RUN_QT_WINDOWS_PLATFORM == '1'
    timeout-minutes: 45
    env:
      QT_QPA_PLATFORM: windows
      QT_OPENGL: software

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install deps
        shell: pwsh
        run: |
          python -m pip install -e ".[dev]"
          python -m pip install pytest pytest-xdist

      - name: Ensure ProcDump is available
        shell: pwsh
        run: |
          $procdump = Get-Command procdump.exe -ErrorAction SilentlyContinue
          if (-not $procdump) {
            $procdumpDir = "tools/procdump"
            New-Item -ItemType Directory -Force $procdumpDir | Out-Null
            $zipPath = Join-Path $procdumpDir "procdump.zip"
            Invoke-WebRequest -Uri "https://download.sysinternals.com/files/Procdump.zip" -OutFile $zipPath
            Expand-Archive -Path $zipPath -DestinationPath $procdumpDir -Force
            Add-Content $env:GITHUB_PATH $procdumpDir
          }

      - name: Check UI deps
        shell: pwsh
        run: |
          python -c "import importlib.util, sys; sys.exit(0 if importlib.util.find_spec('PySide6') else 1)"
        continue-on-error: true

      - name: Pytest UI
        shell: pwsh
        run: |
          ./scripts/run_qml_tests_windows.ps1 -QtQpaPlatform "windows" -QtOpenGL "${{ env.QT_OPENGL }}"

      - name: Upload QML diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: qml-test-diagnostics-windows
          path: |
            test-results/qml/**
            var/crashdumps/**
            var/crashdumps/**/*.dmp
          if-no-files-found: warn
          retention-days: 5
