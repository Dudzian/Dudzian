# Configuration for developer tooling access
[features]
# Allow the agent to run shell/exec commands via streamable shell support.
streamable_shell = true
# Permit issuing web search requests when available.
web_search_request = true
