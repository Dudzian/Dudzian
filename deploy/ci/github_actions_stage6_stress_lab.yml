name: Stage6 stress lab

on:
  workflow_dispatch:
  schedule:
    - cron: '20 3 * * 2'

jobs:
  stage6-stress-lab:
    name: Stage6 Stress Lab acceptance
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      PYTHONUNBUFFERED: "1"
      STRESS_LAB_OUTPUT: var/audit/stage6/stress_lab
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install '.[dev]'

      - name: Prepare directories
        run: |
          mkdir -p "$STRESS_LAB_OUTPUT"
          mkdir -p var/audit/stage6/market_intel
          mkdir -p var/stage6/market_intel

      - name: Populate Market Intel dataset
        env:
          MARKET_INTEL_SQLITE_PROVIDER: stage6_samples.market_intel:build_provider
        run: |
          python scripts/build_market_intel_metrics.py \
            --mode sqlite \
            --config config/core.yaml \
            --populate-sqlite \
            --sqlite-path var/stage6/market_intel/market_metrics.sqlite \
            --output-dir var/audit/stage6/market_intel \
            --manifest var/audit/stage6/market_intel/manifest.json

      - name: Generate signing key
        id: signing
        run: |
          KEY=$(python - <<'PY'
import secrets
print(secrets.token_hex(32))
PY
)
          echo "STRESS_LAB_SIGNING_KEY=$KEY" >> "$GITHUB_ENV"

      - name: Run Stress Lab
        run: |
          python scripts/run_stress_lab.py \
            --config config/core.yaml \
            --output "$STRESS_LAB_OUTPUT/stress_lab_report.json"

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: stage6-stress-lab-${{ github.run_number }}
          path: |
            ${{ env.STRESS_LAB_OUTPUT }}/

      - name: Upload Market Intel baselines
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: stage6-market-intel-${{ github.run_number }}
          path: |
            var/audit/stage6/market_intel/manifest.json
            var/audit/stage6/market_intel/*.json

      - name: Publish summary
        if: always()
        run: |
          if [ -f "$STRESS_LAB_OUTPUT/stress_lab_report.json" ]; then
            echo '### Stress Lab report' >> "$GITHUB_STEP_SUMMARY"
            cat "$STRESS_LAB_OUTPUT/stress_lab_report.json" >> "$GITHUB_STEP_SUMMARY"
          fi
