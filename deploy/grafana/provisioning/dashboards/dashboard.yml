apiVersion: 1

datasources: []

dashboardProviders:
  - name: kryptolowca
    orgId: 1
    folder: bot_core
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
