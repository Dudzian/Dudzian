# Zależności wymagane do przygotowania bundla desktopowego w środowiskach offline.
#
# Plik zakłada, że pakiet `dudzian-bot` instalujemy z lokalnego koła / archiwum
# zbudowanego przez `pip wheel .` i udostępnionego w katalogu `dist/`.
# Dokładne wersje transytywnych zależności znajdują się w
# `deploy/packaging/requirements-desktop.lock`.
numpy==1.26.4
pandas==2.2.2
joblib==1.4.2
pyinstaller==6.5.0
briefcase==0.3.18
